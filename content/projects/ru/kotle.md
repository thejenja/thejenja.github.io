---
title: "Kotle"
description: "Продвинутый Userscript для OpenVK"
color: "#ef4444"
icon: "🧩"
featured: true
slug: "kotle"
type: "userscript"
stage: "completed"
technologies: ["javascript", "tampermonkey", "dom-manipulation"]
date: 2025-01-22
linksTop:
  [
    {
      "label": "GitHub",
      "href": "https://github.com/thejenja/kotle",
      "icon": "github",
    },
  ]
---

# Kotle: Выход за рамки CSS

Если темы оформления меняют только "косметику", то **Kotle** меняет правила игры. Это мощный JavaScript-скрипт (Userscript), который внедряется в код страницы и модифицирует её поведение на лету. Вдохновленный легендарным `VKify`, Kotle предоставляет пользователю инструменты бога, недоступные в обычных настройках.

## JS-магия в браузере

Kotle работает через менеджеры скриптов вроде **Tampermonkey**. Это позволяет выполнять произвольный JavaScript код при загрузке страницы.
Что умеет Kotle:
- **DOM-манипуляции**: Скрипт находит элементы страницы (меню, кнопки, блоки) и перестраивает их. Удаляет лишнее, добавляет новое.
- **Кастомные галочки**: Хотите верификацию, но администрация не дает? Kotle отрисует галочку локально, используя SVG-инъекции.
- **Панель настроек**: Я реализовал собственный UI настроек, который встраивается прямо в интерфейс сайта, позволяя включать и выключать функции в реальном времени.

## Архитектура

Написание userscript'ов — это всегда работа с "живым" организмом чужого сайта. Классы могут меняться, элементы загружаться асинхронно. Поэтому в Kotle активно используются `MutationObserver` для отслеживания изменений в DOM-дереве и реакция на динамическую подгрузку контента (SPA-переходы).

## Значение

Kotle — это проект для power-user'ов. Он показывает, что веб — это открытая платформа. Даже если разработчики сайта не предусмотрели какую-то функцию, мы можем добавить её сами с помощью JavaScript. Это манифест свободы пользователя в настройке своего цифрового пространства.