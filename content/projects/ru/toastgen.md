---
title: "ToastGen"
description: "Генератор системных уведомлений на Python"
color: "#eab308"
icon: "🍞"
slug: "toastgen"
type: "desktop-app"
stage: "archived"
technologies: ["python", "ttkbootstrap", "tkinter", "windows-api"]
date: 2022-09-12
linksTop:
  [
    {
      "label": "GitHub",
      "href": "https://github.com/thejenja/toastgen",
      "icon": "github",
    },
  ]
---

# ToastGen: Первые шаги в GUI

У каждого разработчика есть свой "Hello World", который вышел из-под контроля. Для меня таким проектом стал **ToastGen**. Это не просто программа, это памятник моему пути изучения Python и создания графических интерфейсов.

## Идея и Реализация

В 2022 году стандартный `tkinter` казался мне скучным и серым (буквально). Я искал способы сделать Python-приложения красивыми и наткнулся на библиотеку **ttkbootstrap**. Она позволяла "натянуть" современные Bootstrap-темы на старый добрый `tk`.

ToastGen родился из простого вопроса: *"А как программы показывают эти красивые уведомления в углу экрана Windows?"*. Вместо того чтобы гуглить ответ, я решил написать утилиту, которая это делает.

Программа позволяет пользователю:
1. Ввести заголовок и текст уведомления.
2. Выбрать стиль оформления (Success, Warning, Danger).
3. Нажать кнопку и увидеть нативное Windows-уведомление (Toast).

## Технические детали

Под капотом используется библиотека `win10toast` (или её аналоги в связке с API системы) для вызова уведомлений и `ttkbootstrap` для отрисовки самого окна генератора.

Код проекта прост и наивен, как и полагается учебному проекту. Но именно здесь я впервые столкнулся с понятиями **Event Loop**, **Callback-функции** и **Layout Management** (знаменитый `.pack()` и `.grid()`). Это был полигон, где я учился связывать логику backend'а (Python скрипт) с frontend'ом (окно приложения).

## Значение проекта

Сегодня ToastGen может показаться примитивным, но для меня он важен как точка отсчета. Это момент перехода от "решения задачек в консоли" к созданию чего-то, что можно потрогать мышкой. Это напоминание о том, что даже самый сложный путь начинается с простого всплывающего уведомления: *"Hello, Developer!"*.